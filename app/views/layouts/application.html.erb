<!DOCTYPE html>
<html>
<head>
  <title>ScreenJedi</title>
  <!--[if lt IE9]>
	<script src="http://html5shim.googlecode.com/svn/trunk/html5.js"
type="text/javascript"></script>
	<![endif]-->
  <%= stylesheet_link_tag "application", :media => "all" %>
  <link href='http://fonts.googleapis.com/css?family=Open+Sans:400,600' rel='stylesheet' type='text/css'>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= javascript_include_tag 'http://ajax.googleapis.com/ajax/libs/jquery/1.4.2/jquery.min.js' %>  
  <%= javascript_include_tag 'http://cdn.pandastream.com/u/1.4/jquery.panda-uploader.min.js' %>
</head>
</head>
<body class="group">
	<div class="container">
	  <nav class="group">
			<h1><%= link_to "ScreenJedi", root_url %></h1>
        <ul>
          <% if session[:user_id] && User.find(session[:user_id]).admin == true %>
          <% company_id = Company.find(Membership.find_by_user_id(session[:user_id]).company_id) %>
          
        	<li><%= link_to "#{Company.find(company_id).name} Members", "/members/#{company_id.id}" %></li>
        	<li><%= link_to "Create A Screencast", new_screencast_url %></li>
			<li><%= link_to "Add a Member", new_membership_url %></li>
        <% end %>

  			<% if session[:user_id] %> 
  				<li><%=link_to "Sign Out", signout_url %></li>
  			<% else %>
  				<li><%= link_to "Sign Up", signup_url %></li>
  				<li><%=link_to "Sign In", signin_url %></li> 
  			<% end %>

				<li><%=link_to "Contact Us"%></li>
      </ul>
  	</nav>

		<%= yield %>
	</div> <!-- container -->
	
	<!-- <footer>
	  <p>About Us</p>
	  ScreenJedi is a web app that allows you to create, post, store and view screencasts.
	</footer> -->
	</body>
</html>

